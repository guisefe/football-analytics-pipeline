name: football_analytics
version: "1.0.0"
config-version: 2

profile: football_analytics

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  football_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts

on-run-start:
  - "{{ log('Starting football analytics pipeline', info=True) }}"

on-run-end:
  - "{{ log('Pipeline complete', info=True) }}"
